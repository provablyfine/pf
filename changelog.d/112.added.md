Native Windows packaging: releases now ship a per-user `pf-setup.exe` installer (PyInstaller-frozen `pf`, Inno Setup) alongside the wheels; documented under "Native Windows" in the getting-started guide.
