Fix SSH agent key adds failing on Windows: Win32-OpenSSH's agent doesn't support constrained (lifetime) adds, so fall back to an unconstrained add there instead of failing `pf login`/`pf ssh` outright.
