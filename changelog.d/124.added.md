A session can now be ended by its owner: `DELETE /session/self`, and `logout()` in the Python client. Requests signed with an ended session get a 401 "Session key is logged out", which the TUI and CLI already handle as a session that needs a new login.
